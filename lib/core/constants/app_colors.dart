import 'package:flutter/material.dart';

class AppColors {
  /// 主色调 - 用于品牌标识、主要按钮、选中状态等
  static const primary = Color(0xffe69e3a);

  /// 次要色调 - 用于主色调的辅助、渐变效果、高亮提示等
  static const secondary = Color(0xFFF0B88A);

  /// 强调色 - 用于特殊强调、标签、徽章等
  static const accent = Color(0xFF7D9B76);

  /// 页面背景色 - 用于整个应用页面的背景（主题色）
  static const background = Color(0xffe69e3a);

  /// 内容背景色 - 用于热门宠物、瀑布流等内容区域背景
  static const contentBackground = Color(0xFFF5F5F5);

  /// 表面色 - 用于卡片、弹窗等容器的背景
  static const surface = Colors.white;

  /// 文字主色 - 用于标题、正文等主要文字
  static const textPrimary = Color(0xFF31261F);

  /// 文字次要色 - 用于辅助文字、提示信息、占位符等
  static const textSecondary = Color(0xFF7A6C62);

  /// 边框色 - 用于分隔线、输入框边框等
  static const border = Color(0xFFE8D9CC);

  /// 成功色 - 用于成功状态提示、勾选图标等
  static const success = Color(0xFF5F7F67);

  /// 卡片浅色 - 用于卡片背景的轻微染色效果
  static const cardTint = Color(0xFFFFF7F1);
}