import 'package:flutter/material.dart';

class AppColors {
  /// 主色调 - 用于品牌标识、主要按钮、选中状态等
  static const primary = Color(0xFFF4C84A);

  /// 次要色调 - 用于主色调的辅助、渐变效果、高亮提示等
  static const secondary = Color(0xFFFFE3A1);

  /// 强调色 - 用于特殊强调、标签、徽章等
  static const accent = Color(0xFFB7791F);

  /// 页面背景色 - 用于整个应用页面的背景
  static const background = Color(0xFFF8F9FB);

  /// 内容背景色 - 用于热门宠物、瀑布流等内容区域背景
  static const contentBackground = Color(0xFFF2F4F7);

  /// 表面色 - 用于卡片、弹窗等容器的背景
  static const surface = Color(0xFFFFFFFF);

  /// 弱化表面色 - 用于输入框、骨架屏等次级容器
  static const surfaceMuted = Color(0xFFF6F7F9);

  /// 浅强调底色 - 只在局部高亮位置使用
  static const surfaceAccent = Color(0xFFFFF5D9);

  /// 文字主色 - 用于标题、正文等主要文字
  static const textPrimary = Color(0xFF1F2937);

  /// 文字次要色 - 用于辅助文字、提示信息、占位符等
  static const textSecondary = Color(0xFF667085);

  /// 边框色 - 用于分隔线、输入框边框等
  static const border = Color(0xFFE4E7EC);

  /// 强调边框色 - 用于较强的结构分隔
  static const borderStrong = Color(0xFFD4D9E1);

  /// 成功色 - 用于成功状态提示、勾选图标等
  static const success = Color(0xFF4F9B69);

  /// 卡片浅色 - 用于图片缺省态等弱存在感底色
  static const cardTint = Color(0xFFF3F5F8);

  /// 占位色 - 用于骨架屏、图片占位等
  static const placeholder = Color(0xFFE7EBF0);

  /// 标签危险色 - 用于价格、促销等强调信息
  static const danger = Color(0xFFE56A54);

  /// 轻提示色 - 用于中性标签、次级强调等
  static const info = Color(0xFF52657A);

  /// 轻提示底色 - 用于标签、输入辅助区等
  static const infoBackground = Color(0xFFF0F4F8);

  /// 阴影色 - 用于卡片阴影等
  static const shadow = Color(0x12182733);

  /// 头部渐变色（保持现有金色系渐变）
  static const headerGradientStart = Color(0xFFF7D88B);
  static const headerGradientMid1 = Color(0xFFD99857);
  static const headerGradientMid2 = Color(0xFF7B4D2E);
  static const headerGradientEnd = Color(0xFF263341);

  /// 头像渐变色
  static const avatarGradientStart = Color(0xFFFFF0C9);
  static const avatarGradientMid = Color(0xFFFFD57D);
  static const avatarGradientEnd = Color(0xFFC78639);

  /// VIP 卡片渐变色
  static const vipCardGradientStart = Color(0xFFFFF4D4);
  static const vipCardGradientMid = Color(0xFFF1D18A);
  static const vipCardGradientEnd = Color(0xFFDFA14D);

  /// 收藏卡片渐变色
  static const favoriteCardGradientStart = Color(0xFFFAF2DF);
  static const favoriteCardGradientMid = Color(0xFFF4E3BB);
  static const favoriteCardGradientEnd = Color(0xFFE9D09D);

  /// 快捷入口颜色
  static const quickActionCustomerService = Color(0xFFE59A58);
  static const quickActionCoupon = Color(0xFFBF7A2F);
  static const quickActionAddress = Color(0xFF5E7A8D);
  static const quickActionShare = Color(0xFF456B5F);

  /// 订单状态颜色
  static const orderPending = Color(0xFFDD7E54);
  static const orderShipping = Color(0xFFCC8D35);
  static const orderReceiving = Color(0xFF5F7FA7);
  static const orderCompleted = Color(0xFF4F9B69);

  /// 装饰色
  static const badgeGold = Color(0xFFFFE5A1);
  static const favoriteIcon = Color(0xFF9A6224);
  static const vipTextDark = Color(0xFF69431B);
  static const vipTextMedium = Color(0xFF6B4C24);
  static const vipTextLight = Color(0xFF5C4728);
  static const favoriteTextDark = Color(0xFF523514);
  static const favoriteTextMedium = Color(0xFF77552B);
}
